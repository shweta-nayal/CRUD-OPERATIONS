<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.3/css/bootstrap.min.css"
        integrity="sha512-jnSuA4Ss2PkkikSOLtYs8BlYIeeIK1h99ty4YfvRPAlzr377vr3CXDb7sb7eEEBYjDtcYj+AjBH3FLv5uSJuXg=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.1/css/all.min.css"
        integrity="sha512-5Hs3dF2AEPkpNAR7UiOHba+lRSJNeM2ECkwxUIxC1Q/FLycGTbNapWXB4tP889k5T5Ju8fs4b1P5z/iB4nMfSQ=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://cdn.datatables.net/v/bs4/dt-2.1.8/datatables.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #1e2a38;
            color: #e0f7fa;
            font-family: 'Arial', sans-serif;
        }

        nav.navbar {
            background-color: #000000 !important; /* Black color */
            height: 80px; 
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
        }


        .navbar-brand,
        .nav-link {
            color: #e0f7fa !important;
            font-size: 1.2rem;
            /* Slightly larger font */
        }

        .navbar-brand:hover,
        .nav-link:hover {
            color: #80deea !important;
        }

        .navbar-nav .nav-item {
            margin-right: 20px;
            /* Spacing between nav items */
        }

        .navbar-nav .nav-link {
            padding: 10px 15px;
            border-radius: 8px;
            /* Rounded edges for links */
            transition: background-color 0.3s ease;
        }

        .navbar-nav .nav-link:hover {
            background-color: #3e8e9d;
            /* Subtle hover effect */
            color: #ffffff !important;
        }

        /* Glass effect for the table */
        .table-responsive {
            background: rgba(255, 255, 255, 0.1); /* Transparent background */
            backdrop-filter: blur(10px); /* Glass blur effect */
            border-radius: 30px; /* Rounded corners for the table */
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Subtle shadow */
            padding: 20px;
            overflow: hidden;
            
        }

        /* Table Styling */
        .table {
            color: #ffffff; /* White text for body */
            width: 100%; /* Ensure the table spans full width */
            table-layout: fixed; /* Ensures columns are evenly distributed */
            border-collapse: collapse; /* Avoid double borders */
            margin: 0 auto; /* Center the table on the page */
        }

        /* Equal height for all table rows */
        .table tbody tr {
            height: 60px; /* Set uniform row height */
        }

        /* Table heading customization */
        .table th {
            background-color: #176b79; /* Pastel blue background */
            color: #ffffff; /* White text color */
            text-align: center; /* Center text in header */
            font-weight: bold; /* Bold header text */
            font-size: 1.1rem; /* Slightly larger text size */
            text-transform: uppercase; /* Capitalize all header text */
            border-right: 2px solid #ffffff; /* Add vertical line after each column */
            padding: 12px 15px; /* Padding for heading cells */
        }

        /* Remove the last border of the last column header */
        .table th:last-child {
            border-right: none;
        }

        /* Table body row customization */
        .table td {
            background: rgba(0, 0, 0, 0.2); /* Semi-transparent rows */
            color: white; /* White text for rows */
            text-align: center; /* Center content in rows */
            padding: 12px 15px; /* Padding for table cells */
            border-right: 2px solid #ffffff; /* Add vertical line after each column */
        }

        /* Remove the last border of the last column row */
        .table td:last-child {
            border-right: none;
        }

        /* Specific column width for ID */
        .table td:nth-child(1), .table th:nth-child(1) {
            width: 10%; /* Set width for the ID column */
        }

        /* Distribute the remaining columns */
        .table td:nth-child(n+2), .table th:nth-child(n+2) {
            width: auto; /* Remaining columns take up the remaining space */
        }

        /* Styling for table actions (icons) */
        .table .text-success, .table .text-danger {
            font-size: 1.2rem; /* Increase icon size */
            padding: 10px;
            border-radius: 50%; /* Round the icons */
        }

        /* Spacing between table heading and body */
        .table th, .table td {
            padding: 12px 15px;
        }


    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a href="/" class="navbar-brand">
                <i class="fas fa-code me-2"></i> DemoProject
            </a>
            <button class="navbar-toggler" data-bs-target="#my-nav" data-bs-toggle="collapse"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div id="my-nav" class="collapse navbar-collapse">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="/"><i class="fas fa-home me-1"></i> Home</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="/add"><i class="fas fa-user-plus me-1"></i> Add User</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="#"><i class="fas fa-globe me-1"></i> About</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="#"><i class="fas fa-envelope me-1"></i> Contact</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</body>

</html>